import { set_last_value, get_last_value } from "../../document_ffi.mjs";
import { create_id as unique_id, current_date_string, set_timeout } from "../../utils_ffi.mjs";

export {
  current_date_string,
  get_last_value,
  set_last_value,
  set_timeout,
  unique_id,
};
